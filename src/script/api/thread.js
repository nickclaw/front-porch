var util = require('../util.js'),
    _ = require('lodash'),
    when = require('when');

function Thread(data) {
    _.extend(this, data);
}

_.extend(Thread, {

});

_.extend(Thread.prototype, {

});

Module.exports = Thread;
